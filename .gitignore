node_modules
dist/
dist-ssr/
coverage/
.vite/
*.local
.DS_Store
*.log
npm-debug.log*
.env
.env.*
!.env.example
/artifacts/
/sweeps/
.claude/settings.local.json

# Ephemeral git worktrees for parallel stream agents. Git removes them when the
# agent finishes; each is a full checkout, so committing one nests the repo in
# itself. .claude/ itself is tracked (agents, commands), hence the narrow entry.
.claude/worktrees/
*.tsbuildinfo

# Generated from scripts/backlog.json; GitHub Issues are the source of truth.
# Regenerate locally with: node scripts/seed-github.mjs --render
docs/backlog.md
