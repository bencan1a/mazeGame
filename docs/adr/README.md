# Architecture Decision Records

One file per decision, numbered, never edited after acceptance — superseded by a
new record instead. Create one with `/new-adr` or by copying `0001`.

A decision belongs here when reversing it later would be expensive, or when a
future contributor would otherwise reasonably do the opposite.
